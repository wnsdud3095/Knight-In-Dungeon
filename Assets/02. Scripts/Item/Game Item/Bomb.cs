using UnityEngine;

public class Bomb : MonoBehaviour, IItem
{
    public void Use()
    {
        // EnemyFSM enemies = FindObjectsByType<EnemyFSM>();

        // foreach(EnemyFSM enemy in enemies)
        // {
        //     if(enemy.GetComponent<Renderer>().isVisible)
        //     {
        //         enemy.Die();
        //     }
        // }
    }
}
